# Database
POSTGRES_USER=mwt
POSTGRES_PASSWORD=mwt
POSTGRES_DB=mwt
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis
REDIS_PASSWORD=cambiar_en_produccion
REDIS_URL=redis://redis:6379/1

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT_URL=http://minio:9000
MINIO_REGION_NAME=us-east-1
MINIO_SERVER_URL=https://minio.muito.work
MINIO_BROWSER_REDIRECT_URL=https://console.minio.muito.work

# Django
SECRET_KEY=template-secret-key-change-me
DEBUG=True
ALLOWED_HOSTS=*
CORS_ALLOWED_ORIGINS=https://consola.mwt.one,https://mwt.one,https://www.mwt.one,http://localhost:3000,http://localhost:8080,https://go.ranawalk.com

# Paperless
PAPERLESS_ADMIN_PASSWORD=cambiar_en_produccion

# Notion
NOTION_TOKEN=your-notion-token-here
NOTION_DATABASE_ID=your-database-id-here

# S26: Notificaciones email automáticas
MWT_EMAIL_BACKEND=apps.notifications.backends.SMTPBackend
DEFAULT_FROM_EMAIL=info@mwt.one
MWT_NOTIFICATION_ENABLED=False
CEO_EMAIL=
PORTAL_BASE_URL=https://consola.mwt.one
AWS_SES_REGION=us-east-1

# S26: SMTP Config (notif automáticas y documentos)
EMAIL_HOST=mail.mwt.one
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=info@mwt.one
EMAIL_HOST_PASSWORD=

# S28-GH-SYNC: Knowledge service — sync con sjoalfaro/mwt-knowledge-hub
# OpenAI + Anthropic ya deberían existir arriba; si no, agregarlos aquí.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
KNOWLEDGE_INTERNAL_TOKEN=cambiar_en_produccion
KB_PATH=/kb
KB_REPO_URL=https://github.com/sjoalfaro/mwt-knowledge-hub
KB_REPO_BRANCH=main
KB_REPO_SUBDIR=docs
# PAT de GitHub con scope repo:read (Fine-grained: "Contents: Read-only" sobre el repo)
GITHUB_TOKEN=
# Secreto compartido con el webhook del repo (cualquier string aleatorio largo)
GITHUB_WEBHOOK_SECRET=cambiar_en_produccion
